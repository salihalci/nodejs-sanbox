npm install validator

package.json ve package-lock.json dan okuyarak bağımlılıkları tekrar yükler.
npm install


GLOBAL module install
Do not need to restart app at the every code change. It will restart automatically.
npm install nodemon@2.0.12 -g